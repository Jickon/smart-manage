<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sm.domain.sys.base.role.mapper.RoleMapper">
    <select id="selectUserRoleNumbers" resultType="java.lang.String">
        SELECT DISTINCT a.number
        FROM t_sys_role a
        INNER JOIN t_sys_user_role b ON b.role_id = a.id
        WHERE b.user_id = #{userId}
          AND b.org_id = #{orgId}
        ORDER BY a.number
    </select>
</mapper>
